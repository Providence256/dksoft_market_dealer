import 'package:flutter/material.dart';

class CommandesScreen extends StatelessWidget {
  const CommandesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Commandes')));
  }
}
